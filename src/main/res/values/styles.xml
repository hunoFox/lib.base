<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 网络请求等待样式 -->
    <style name="ProgressHUD" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- 弹框对话框的样式 -->
    <style name="Dialog" parent="@style/Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@drawable/shape_dialog_theme</item>
        <item name="android:colorBackground">@drawable/shape_dialog_theme</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="DialogAnimStyle">
        <item name="android:windowEnterAnimation">@anim/anim_dialog_show</item>
        <item name="android:windowExitAnimation">@anim/anim_dialog_dismiss</item>
    </style>

    <!-- 分割线样式 -->
    <style name="Divider">
        <item name="android:background">@color/divider</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1.0px</item>
    </style>
    <style name="Divider_portrait">
        <item name="android:background">@color/divider</item>
        <item name="android:layout_width">1.0px</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <!--大button的样式-->
    <style name="big_button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">16sp</item>
        <item name="android:background">@drawable/selector_big_btn</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="downLoadProgressBar">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:progressDrawable">@drawable/progress_bar_shape</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:minHeight">10dip</item>
        <item name="android:maxHeight">10dip</item>
    </style>

    <!--wheelView的主题-->
    <style name="blend_theme_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:width">800px</item>
        <item name="android:height">700px</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <!--checkBox样式-->
    <style name="CheckBox" parent="@android:style/Widget.Material.CompoundButton.CheckBox" >
        <item name="android:colorControlActivated">@color/colorPrimary</item>
        <item name="android:colorControlNormal">@color/text_gray</item>
    </style>

</resources>